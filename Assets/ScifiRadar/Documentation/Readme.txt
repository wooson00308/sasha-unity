
Documentation

All the prefabs are ready to use in prefab folder.
Every prefab has animator component atteched to it and all the animations have loop enabled.
Loop can be disabled from the animation's properties.
Default time for every animation is 4s.
Default size of sprites is 512 x 512.
Adjust the size of each prefab according to scene view.

